# ============================================
# Environment Files
# ============================================
# .env.dev   - Development (local Redis, localhost)
# .env.prod  - Production (Docker Redis, Tailscale)
#
# Uso:
# Development:  cp env/.env.dev .env
# Production:   Jenkins maneja env/.env.prod como Secret File
# ============================================
